import { View, Image, Text, TouchableOpacity } from "react-native"
import React, { ReactElement } from "react"

const ChatBoard = ({}): ReactElement => {
    return(
        <View></View>
    );
};

export default ChatBoard;